"""Modular components for long_source tasks.

Each subpackage owns one pipeline role:
    summarizer         — doc-chunk gisting
    unfolder           — selective sub-chunk expansion
    verifier           — gist commit cascade
"""
