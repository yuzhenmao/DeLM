# LLM model configuration.
# Copy this file to config/model_config.yaml (gitignored) and fill in real keys.

models:
  "google/gemini-3-flash-preview":
    api_type: "openai"
    base_url: "https://openrouter.ai/api/v1"
    api_key: "<your OpenRouter (or other OpenAI-compatible) API key>"

  # Example second model (only needed if a preset references it):
  # "openai/gpt-5-mini":
  #   api_type: "openai"
  #   base_url: "https://openrouter.ai/api/v1"
  #   api_key: "<your api key>"
